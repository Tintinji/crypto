# ETH 空方（逻辑链 A）

快照 `2026-09-16T21:13:43Z`。市值排名 `#2`，流通市值与完全稀释估值均为 `$294.18B`。空方只写反面：L1 执行费向 rollup 泄漏、年化链费用撑不起四位数倍数、一年已大幅跑输比特币。本币被当成结算层与抵押品货币来卖，不是可核验的 L1 gas 现金流。

## 1. 这个资产在卖什么，谁付钱？

智能合约 L1 卖的是区块空间、状态承诺与生态 gravitas；付钱的应是终端用户与机器人支付的 gas，以及把流动性锁在这条状态机上的应用。可核验的链费用 30d 仅 `$12.45M`，1y 与年化均为 `$208.33M`。协议家族费用、协议收入、Llama 稳定币流通全部未知，持有人看不到可分配利润，也不能把「结算货币」写成已量化的美元存量。CEX 托管 AUM 未知，不得当成协议锁仓。赛道已写明：应用与稳定币仍在，但 L1 执行费被 rollup 抽走。`$208.33M` 年化费用对 `$294.18B` 市值，市场买的是结算溢价，不是收费权。

## 2. 需求是在扩张还是叙事空转？

一年本币 `-46.40%`，比特币同期 `-34.80%`，相对比特币 `-17.79%`。现价 `$2.41K`，距历史高点 `$4.95K` 回撤 `-51.27%`。三十日本币 `+26.40%`（相对比特币 `+6.67%`）只是短窗反弹，盖不住一年跑输；七日 `-2.30%`、二十四小时 `+0.47%` 也没有扭转费用萎缩。链上 DeFi 锁仓 `$49.02B`、市值/锁仓 `6.00x` 说明应用还在，但三十日费用 `$12.45M` 并未随锁仓扩张。二十四小时成交额 `$15.99B`、成交额/市值 `5.43%` 是换手，不是费用需求回升。协议家族锁仓未知，不能另写一套需求锚。

## 3. 代币如何捕获价值？

市值/完全稀释估值 `1.00`，流通供给与总供给均为 `122.05M`，没有未解锁浮筹，也没有协议家族收入。代币留下多少钱，取决于费用是否销毁或分给持有人；本事实包给不出协议收入，捕获只能对照链费用。完全稀释估值/年化费用为 `1412.11x`。同赛道 SOL 年化链费用 `$241.22M` 已高于以太坊一层，市值仅 `$57.91B`、倍数 `259x`。费用向二层与应用链泄漏，以太坊是主受损方；一层收费权被抽走后，代币留下的是结算叙事，不是现金流。

## 4. 供给何时变差？

最大供给未知，公开接口未提供已验证解锁日历，辩论中不得编造具体解锁美元金额。不可验证性本身是论点：安全预算、质押锁定与净发行无法从本事实包核对。年化链费用 `$208.33M` 若盖不住增发，流通 `122.05M` 会变差而持有人看不见日历。市值/完全稀释估值 `1.00` 只说明当前全流通，不证明供给不会再增，更不证明销毁能对冲通胀。

## 5. 竞争替代成本是什么？

SOL 三十日链费用 `$23.11M`、年化 `$241.22M`；TRX 三十日 `$24.53M`、年化 `$355.61M`，主要来自稳定币转账。以太坊一层三十日 `$12.45M`、年化 `$208.33M`，费用侧已被高吞吐应用链与支付轨道超过。赛道二十一个标的合计市值 `$419.0B`，其余十八个合计约 `$35B`，通用计算过剩；跨链桥与意图层降低迁移成本。`1412x` 相对 SOL `259x` 是货币溢价，溢价要求用户不迁走，而费用已经在迁。Canton 三十日链费用 `$49.6M` 口径须另核，禁止与以太坊放进同一张倍数表就下贵贱结论，更不能给费用回升作担保。

## 6. 估值锚是否过大？

完全稀释估值/年化费用 `1412.11x`（赛道表作 `1412x`）：`$294.18B` 除以 `$208.33M`。差异来自货币溢价，不是会计错误，但溢价已被一年 `-46.40%`、相对比特币 `-17.79%` 压缩。市值/锁仓 `6.00x` 对照 SOL `10.11x` 并不便宜——锁仓 `$49.02B` 锁的是应用流动性，费用已泄漏。周期上 L1 贝塔大于比特币；一年跑输说明市场在重定价「结算货币」，不是锚收窄后仍便宜。

## 7. 未来 12 个月必须发生 / 绝不能发生的事

必须看到年化链费用从 `$208.33M` 回升，且一层不再继续把执行费漏给 rollup；否则 `1412x` 只能靠货币叙事续命。绝不能再出现一年量级的相对比特币跑输（本快照已是本币 `-46.40%`、相对 `-17.79%`）。协议收入与稳定币流通若继续未知，结算货币主张无法被证伪。价格 `$2.41K`、距高点 `-51.27%` 不是安全垫；历史低点 `$0.432979` 只提醒回撤没有地板写在事实包里。

研究备忘，非投资建议。
