# ALGO 辩论（逻辑链 A）

快照 `2026-09-16T21:13:43Z`。排名 `#81`，流通市值 `$795.30M`，FDV `$795.32M`，MC/FDV `1.00`。只引用 `00-facts.md` 与赛道表已给数字；空单元格与「未知」不补数。赛道 21 个标的合计 MC `$419.0B`，ETH 独占 `$294.18B`，其余 18 个合计约 `$35B`，ALGO 以 `$795.30M` 落在该档一小块。赛道点名 SUI / AVAX / NEAR / ADA / DOT / INJ，未点名 ALGO。按长尾通用计算 L1 对打，不把结算货币、高吞吐执行或支付轨道尺子套过来。

## 第 1 轮：各攻对方最弱三点

### 多方攻空方

1. **用价格路径把已核对用量写成空转。** 空方把本币 1y `-63.10%`、相对 BTC `-43.40%`（BTC 同期 1y `-34.80%`）和距 ATH `$3.56` 的 `-97.53%` 写成需求叙事空转。可核验的是链费用 30d `$525.00`、年化与 1y 均为 `$7.11K`，链上 DeFi TVL `$29.15M`（kind=`chain`，Algorand=`$29.15M`）。价格路径证明周期贝塔与重定价，不能单独把这两笔写成用量归零。30d `+12.30%`（相对 `-5.23%`）落后 BTC 30d `+18.50%`，盖不住一年跑输，也不是费用已停。7d `-12.80%`（相对 `-10.38%`）与 BTC `-2.70%` 同向。24h `-1.60%` 对 BTC `+0.57%` 是方向噪声，相对值本包记为未给，不得自算。24h 成交额 `$43.62M`、成交额/MC `5.48%` 是二级定价：空方用它否定需求扩张可以，但不能用换手当「生态已空」。稳定币流通未知，空方不得把未知补成流失。现价 `$0.087953` 贴近 ATL `$0.075732` 是价格路径，不是用量归零。

2. **「必须按 `$7.11K` 续命」错把 gas 当定价分子。** 双方已承认 ALGO 不是结算货币、不是高吞吐执行、也不是支付轨道。空方一面写 `111809.10x` 大于 ETH `1412x`、SOL `259x`，一面要求年化 `$7.11K` 守住否则倍数靠叙事续命，等于仍把长尾 gravitas 改回现金流锚。赛道读法：ETH `1412x` 买结算溢价，SOL `259x` 买执行租金；TVL 低于 `$1B` 还要 `$3B+` 市值才默认叙事溢价。分子是 `#81` 与 `$795.30M`，未过该门槛，不得套用该默认句。分母不是年化 `$7.11K`。协议收入未知，不得把未知 take rate 补成捕获失败。赛道禁止据此与 ETH 并表下贵贱结论。Canton 30d `$49.6M` 禁止并表。

3. **「换一条通用计算链即可」缺迁出额。** 赛道点名 SUI / AVAX / NEAR / ADA / DOT / INJ 抢同一类开发者，未点名 ALGO；跨链桥与意图层降低迁移成本。facts 没有给出 `$29.15M` TVL 的迁出序列。SOL 年化 `$241.22M`、TRX `$355.61M`、ETH L1 `$208.33M` 说明费用不在同一量级，不等于本链锁仓已被替代。30d 费用 `$525.00` 落在其余 18 个「多数 <$1M/30d」，是长尾费用档，不是迁出证明。搬迁成本是位次、`$795.30M` 与已核对 TVL，不是换链口号。

### 空方攻多方

1. **「TVL 与费用仍在=需求未空转」超证。** 多方用 `$29.15M` / `$7.11K` 压过空转指控。现价 `$0.087953`，距 ATH `$3.56` 为 `-97.53%`，1y `-63.10%`；30d `+12.30%`（相对 `-5.23%`）落后比特币，盖不住一年跑输，也不能把短窗正收益写成用量扩张。7d `-12.80%`（相对 `-10.38%`）与 BTC 同向收缩。24h `-1.60%`，相对值本包未给。MC/TVL `27.28x` 对照 ETH `6.00x`、SOL `10.11x`，不在「有生态」区间。稳定币流通、协议家族费用与协议家族 TVL 未知，禁止补成份额。`$43.62M` 日量、成交额/MC `5.48%` 是换手，不是费用扩张。CEX 托管 AUM 未知，不得当成协议锁仓。

2. **「#81 / 未过 $3B+ / 111809.10x 是 gravitas=已定价」循环。** 多方用市值位次、未过叙事桶与「通用 L1 溢价」解释市值。`$795.30M` 占其余 18 个约 `$35B` 的一小块，不能自证 `27.28x` 合理，也不能用「禁止并表」把 `111809.10x` 写成不贵。`27.28x` 的分母是 `$29.15M`，绝对锁仓比 ETH `$49.02B`、SOL `$5.73B` 低两个数量级以上。现价相对 ATH `-97.53%`、1y `-63.10%` 已切一轮周期溢价，仍按五位数收费权与高于「有生态」区间的锁仓尺计价，不是锚已收到费用能解释的位置。MC/FDV `1.00`、流通与总供给均为 `9.04B` 只说明全流通，硬顶 `10.00B` 仍有缺口，不证明 gravitas 对得上 `$795.30M`。

3. **「价值留在原生 gas / 硬顶+全流通=供给安全」缺捕获与供给锚。** 协议家族费用与收入未知，链费用不等于代币收费权。解锁日历未知，不得编造到期美元。流通 `9.04B` 等于总供给、MC/FDV `1.00` 只排除未流通折价库存，不证明硬顶 `10.00B` 对流通的缺口不会进入，也不证明 `$7.11K` 年化费用能对冲净发行。多方「绝不能按 `$7.11K` 给 FDV 定价」把对手错锚写成安全垫；ATL `$0.075732` 提醒事实包没有地板。年化 `$7.11K` 远低于 ETH `$208.33M`、SOL `$241.22M`、TRX `$355.61M`，不能对冲硬顶缺口。

## 第 2 轮：六点标 承认 / 反驳 / 降级

1. **价格路径=用量空转或用量扩张：反驳双向超证，并降级空方措辞。** 空方须把「叙事空转」降级为「价格重定价，费用与 TVL 仍在」。多方同时承认：`$29.15M` / `$7.11K` 只证明锁仓与链费用未消失，不证明需求在扩张；1y `-63.10%` / 相对 `-43.40%` 双方承认，相对跑输不是用量归零，三十日相对跑输也不是用量扩张。30d `+12.30%` 可逆，不能写成费用表爆发，也不能回冲一年本币为负。空方亦不得把距 ATH `-97.53%` 或贴近 ATL `$0.075732` 写成 TVL 已撤。

2. **111809.10x 对 1412x/259x=纯贵：承认 gas 锚过大，降级「必须按费用定价」与「位次/低于叙事桶证成」。** 双方承认按 L1 gas 估，`111809.10x` 大于 ETH `1412x`、SOL `259x`，作为现金流锚过大。空方「必须按 `$7.11K` 给 `$795.32M` 定价、把 `111809.10x` 读成纯贵」降级为错锚条件——双方已声明不卖结算/执行/支付轨。多方「`#81` 与长尾一小块撑住分子」与「未过 `$3B+` 故不得套默认叙事句」一并降级：不能用市值证明市值，也不能用结算/执行倍数给通用计算开脱或称不贵。赛道原话：差异来自溢价读法，不是会计错误；TVL 低于 `$1B` 还要 `$3B+` 市值才默认叙事溢价，本币 `$795.30M` 未过该门槛，不得套用该默认句。

3. **MC/TVL 27.28x 过大：承认空方，降级「有用量即有生态」。** 双方承认 `27.28x` 高于 ETH `6.00x`、SOL `10.11x`，不在「有生态」区间。多方「`$29.15M` 已核对」降级为「有锁仓、无生态倍数」；空方不得把 `27.28x` 写成 TVL 为零，也不得把未过 `$3B` 的分子改写成「锁仓低于 `$1B` 却要 `$3B+`」。协议家族 TVL 未知，不能另写粘性或流失。其余 18 个合计约 `$35B` 不能反向证明 `$795.30M` 便宜。

4. **30d 与日量=需求扩张：承认多方超证。** 多方承认 `$43.62M` / `5.48%` 是换手，30d `+12.30%`（相对 `-5.23%`）可逆且落后 BTC，不能覆盖 1y `-63.10%`。7d `-12.80%`（相对 `-10.38%`）对 BTC 7d `-2.70%` 同向收缩，24h `-1.60%` 对 BTC `+0.57%` 相对值未给，不是独立需求脉冲。空方降级：不能因短窗与一年跑输主张 `$29.15M` 与 `$7.11K` 已失效。

5. **换链即可=已被替代：降级。** 双方承认通用计算拥挤：SUI / AVAX / NEAR / ADA / DOT / INJ 抢开发者，赛道未点名 ALGO。空方「换一条即可」缺迁出额，降级为「迁移成本下降，TVL 迁出未给出」。多方「`#81` + `$795.30M` + `$29.15M` 即搬迁成本」同样缺序列，降级为存量位次，不是不可替代证明。Canton 不得并表。TRX/SOL 费用领先不得写成同一溢价。

6. **硬顶/全流通=供给安全 / gas=捕获：承认双方都缺锚。** 双方承认协议收入、稳定币流通、解锁日历、协议家族 TVL、CEX AUM 未知。MC/FDV `1.00`、流通与总供给 `9.04B` 只排除未流通库存。硬顶 `10.00B` 只封上限。多方「硬顶可核对、无库存折价」降级为结构陈述，不能把全流通或硬顶补成供给封闭；空方「看不见日历即供给变差」降级为数据缺口，不得编造解锁美元。`$7.11K` 能否对冲硬顶缺口进入不可核验。ATL `$0.075732` 只作历史下沿。

研究备忘，非投资建议。
