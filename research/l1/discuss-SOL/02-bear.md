# SOL 空方（逻辑链 A）

快照 `2026-09-16T21:13:43Z`。市值排名 `#7`，流通市值 `$57.91B`，完全稀释估值 `$62.55B`。空方只写反面：本币卖的是高吞吐执行，不是结算货币溢价；一年已大幅跑输比特币；市值/锁仓高于以太坊，费用捕获与供给日历均不可核验。

## 1. 这个资产在卖什么，谁付钱？

智能合约 L1 卖区块空间、状态承诺与生态 gravitas。赛道已声明：SOL 按「高吞吐应用链」定价，不是结算货币，也不是支付轨道。付钱的应是终端用户与机器人的 gas。可核验链费用 30d `$23.11M`，1y 与年化均为 `$241.22M`。协议家族费用、协议收入、Llama 稳定币流通全部未知，持有人看不到可分配利润，也不能把美元结算存量写成已量化需求。CEX 托管 AUM 未知，不得当成协议锁仓。应用层可以把价值留在自己的 token，链费用不等于代币收费权。

## 2. 需求是在扩张还是叙事空转？

一年本币 `-58.50%`，比特币同期 `-34.80%`，相对比特币 `-36.35%`。现价 `$98.62`，距历史高点 `$293.31` 回撤 `-66.38%`。三十日本币 `+30.20%`（相对比特币 `+9.87%`）只是短窗反弹，盖不住一年跑输；七日 `-3.60%`（相对 `-0.92%`）、二十四小时 `+1.63%` 也没有扭转年度收缩。链上 DeFi 锁仓 `$5.73B`、市值/锁仓 `10.11x`，对照以太坊 `6.00x` 更贵。二十四小时成交额 `$3.47B`、成交额/市值 `6.00%` 是换手，不是费用需求扩张。协议家族锁仓未知，不能另写一套需求锚。稳定币流通未知，禁止把未知补成成长。

## 3. 代币如何捕获价值？

协议家族费用与协议收入全部未知，不得编造销毁、分红或回购。代币留下多少钱，取决于费用是否销毁或分给持有人；本事实包只能对照链费用。完全稀释估值/年化费用 `259.31x`（`$62.55B` 除以 `$241.22M`）。市值/完全稀释估值 `0.93`，流通供给 `587.15M`、总供给 `634.21M`，仍有未流通浮筹。年化费用高于以太坊一层 `$208.33M`，只说明执行吞吐在收费，不证明 SOL 持有人拿到 take rate。`$3.47B` 日成交是二级换手，不是持有人票息。

## 4. 供给何时变差？

最大供给未知，公开接口未提供已验证解锁日历，辩论中不得编造具体解锁美元金额。不可验证性本身是论点：通胀能否对冲 `$241.22M` 年化费用，无法从本包核对。市值/完全稀释估值 `0.93` 说明尚未全流通，流通 `587.15M` 对总供给 `634.21M` 的缺口是已知浮筹，但释放时点未知。未知硬顶意味着安全预算与净发行都看不见日历。

## 5. 竞争替代成本是什么？

通用计算过剩：SUI、AVAX、NEAR、ADA、DOT、INJ 抢同一类开发者，跨链桥与意图层降低迁移成本。赛道二十一个标的合计市值 `$419.0B`，其余十八个合计约 `$35B`。TRX 三十日链费用 `$24.53M`、年化 `$355.61M` 已高于 SOL，且是支付轨道，不是通用计算溢价。以太坊一层三十日仅 `$12.45M`、年化 `$208.33M`，执行费已漏向 rollup；SOL 费用更高，说明它处在被拥挤的执行层，不是不可替代的结算位。以太坊市值 `$294.18B` 买的是结算货币；SOL 没有该溢价，市值约其五分之一，替代成本只是换一条高吞吐链。Canton 三十日链费用 `$49.6M` 口径须另核，禁止并表下贵贱结论。

## 6. 估值锚是否过大？

完全稀释估值/年化费用 `259.31x`，赛道表作 `259x`。差异被说成货币溢价，但 SOL 明确不是结算货币，应用链却按数百倍收费权计价。完全稀释估值 `$62.55B` 高于流通市值 `$57.91B`，分子还含未流通部分。市值/锁仓 `10.11x` 高于以太坊 `6.00x`：`$57.91B` 市值只对应 `$5.73B` 锁仓。一年 `-58.50%`、距高点 `-66.38%` 已压缩一轮，仍未把锚收到费用能解释的位置。周期上 L1 贝塔大于比特币；相对比特币 `-36.35%` 说明市场在重定价高吞吐叙事，不是锚已经便宜。

## 7. 未来 12 个月必须发生 / 绝不能发生的事

必须看到年化链费用守住或高于 `$241.22M`，且 `$5.73B` 锁仓不被抽走，否则 `259.31x` 与 `10.11x` 只能靠叙事续命。绝不能再出现一年量级的相对比特币跑输（本快照已是本币 `-58.50%`、相对 `-36.35%`）。协议收入、稳定币流通与解锁若继续未知，捕获与供给主张无法被证伪。价格 `$98.62`、距高点 `-66.38%` 不是安全垫；历史低点 `$0.500801` 只提醒回撤没有地板写在事实包里。

研究备忘，非投资建议。
