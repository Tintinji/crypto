# ADA 空方（逻辑链 A）

快照 `2026-09-16T21:13:43Z`。市值排名 `#19`，流通市值 `$7.32B`，完全稀释估值 `$8.78B`。先声明：本币卖的是通用计算区块空间，不是结算货币、高吞吐执行，也不是支付轨道。空方只写反面：年化链费用 `$1.15M`、市值/锁仓 `140.67x`、一年相对比特币 `-65.95%`。

## 1. 这个资产在卖什么，谁付钱？

智能合约 L1 卖区块空间、状态承诺与生态 gravitas。付钱的应是终端用户与机器人的 gas。可核验链费用 30d 仅 `$43.03K`，1y 与年化均为 `$1.15M`。协议家族费用、协议收入、Llama 稳定币流通全部未知，持有人看不到可分配利润。CEX 托管 AUM 未知，不得当成协议锁仓。赛道把 ADA 列进通用计算过剩。`$7.32B` 市值对照的是链费用口径，不是持有人收费权。

## 2. 需求是在扩张还是叙事空转？

一年本币 `-77.80%`，比特币同期 `-34.80%`，相对比特币 `-65.95%`。现价 `$0.195069`，距历史高点 `$3.09` 回撤 `-93.68%`。三十日本币 `+12.50%`（相对 `-5.06%`）盖不住一年跑输；七日 `-9.00%`（相对 `-6.47%`）、二十四小时 `-0.43%` 也未扭转收缩。链上 DeFi 锁仓 `$52.03M`、市值/锁仓 `140.67x`，对照以太坊 `6.00x`、SOL `10.11x`：锁仓远低于 `$1B` 仍要 `$3B+` 市值，赛道默认叙事溢价。二十四小时成交额 `$428.61M`、成交额/市值 `5.86%` 是换手。协议家族锁仓与稳定币流通未知，禁止补成成长。

## 3. 代币如何捕获价值？

协议家族费用与协议收入全部未知，不得编造销毁、分红或回购。捕获取决于费用是否销毁或分给持有人；本包只能对照链费用。完全稀释估值/年化费用 `7623.92x`。市值/完全稀释估值 `0.83`，流通 `37.52B`、总供给与最大供给均为 `45.00B`。年化费用远低于以太坊一层 `$208.33M`、SOL `$241.22M`、TRX `$355.61M`。日成交 `$428.61M` 是二级换手，不是票息。

## 4. 供给何时变差？

公开接口未提供已验证解锁日历，不得编造解锁美元金额。不可验证性本身是论点。市值/完全稀释估值 `0.83` 说明尚未全流通，流通 `37.52B` 对总供给 `45.00B` 的缺口是已知浮筹。硬顶 `45.00B` 只封上限，不证明未流通部分何时进入，也不证明 `$1.15M` 年化费用能对冲净发行。

## 5. 竞争替代成本是什么？

通用计算过剩：SUI、AVAX、NEAR、ADA、DOT、INJ 抢同一类开发者，跨链桥与意图层降低迁移成本。赛道二十一个标的合计市值 `$419.0B`，其余十八个合计约 `$35B`。SOL 三十日链费用 `$23.11M`、年化 `$241.22M`；TRX 三十日 `$24.53M`、年化 `$355.61M`；以太坊一层三十日 `$12.45M`、年化 `$208.33M`。本链三十日仅 `$43.03K`、锁仓 `$52.03M`，替代成本只是换一条通用计算链。Canton 三十日 `$49.6M` 口径须另核，禁止并表。

## 6. 估值锚是否过大？

完全稀释估值/年化费用 `7623.92x`（`$8.78B` 除以 `$1.15M`），远高于以太坊 `1412x`、SOL `259x`。市值/锁仓 `140.67x`：`$7.32B` 只对应 `$52.03M` 锁仓。一年 `-77.80%`、距高点 `-93.68%` 已压缩一轮，仍未收到费用能解释的位置。周期上 L1 贝塔大于比特币；相对比特币 `-65.95%` 是在重定价通用计算叙事。

## 7. 未来 12 个月必须发生 / 绝不能发生的事

必须看到年化链费用守住或高于 `$1.15M`，且 `$52.03M` 锁仓不被抽走，否则 `7623.92x` 与 `140.67x` 只能靠叙事续命。绝不能再出现一年量级相对比特币跑输（本快照本币 `-77.80%`、相对 `-65.95%`）。协议收入、稳定币流通与解锁若继续未知，捕获与供给主张无法被证伪。价格 `$0.195069`、距高点 `-93.68%` 不是安全垫；历史低点 `$0.019253` 只提醒回撤没有地板写在事实包里。

研究备忘，非投资建议。
