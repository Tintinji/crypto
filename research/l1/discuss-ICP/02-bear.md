# ICP 空方（逻辑链 A）

快照 `2026-09-16T21:13:43Z`。市值排名 `#59`，流通市值与完全稀释估值均为 `$1.41B`。先声明：本币卖的是通用计算区块空间，不是结算货币、高吞吐执行，也不是支付轨道。空方只写反面：年化链费用 `$1.98M`、市值/锁仓 `103.48x`、一年相对比特币 `-18.87%`、距历史高点 `-99.64%`。

## 1. 这个资产在卖什么，谁付钱？

智能合约 L1 卖区块空间、状态承诺与生态 gravitas。付钱的应是终端用户与机器人的 gas。可核验链费用 30d 仅 `$175.95K`，1y 与年化均为 `$1.98M`。协议家族费用、协议收入、Llama 稳定币流通全部未知，持有人看不到可分配利润。CEX 托管 AUM 未知，不得当成协议锁仓。赛道把通用计算标成过剩。`$1.41B` 市值对照的是链费用口径，不是持有人收费权。

## 2. 需求是在扩张还是叙事空转？

一年本币 `-47.10%`，比特币同期 `-34.80%`，相对比特币 `-18.87%`。现价 `$2.54`，距历史高点 `$700.65` 回撤 `-99.64%`。三十日本币 `+12.60%`（相对 `-4.98%`）落后比特币 `+18.50%`，盖不住一年跑输；七日 `-10.60%`（相对 `-8.12%`）、二十四小时 `+2.91%` 也未扭转收缩。链上 DeFi 锁仓 `$13.66M`、市值/锁仓 `103.48x`，对照以太坊 `6.00x`、SOL `10.11x`。二十四小时成交额 `$39.87M`、成交额/市值 `2.82%` 是换手。协议家族锁仓与稳定币流通未知，禁止补成成长。

## 3. 代币如何捕获价值？

协议家族费用与协议收入全部未知，不得编造销毁、分红或回购。捕获取决于费用是否销毁或分给持有人；本包只能对照链费用。完全稀释估值/年化费用 `712.22x`。市值/完全稀释估值 `1.00`，流通供给与总供给均为 `556.87M`。年化费用远低于以太坊一层 `$208.33M`、SOL `$241.22M`、TRX `$355.61M`。日成交 `$39.87M` 是二级换手，不是票息。

## 4. 供给何时变差？

最大供给未知，公开接口未提供已验证解锁日历，不得编造解锁美元金额。不可验证性本身是论点。市值/完全稀释估值 `1.00` 只说明流通等于总供给，不证明没有净发行，也不证明 `$1.98M` 年化费用能对冲未知通胀。未知硬顶意味着安全预算与净发行都看不见日历。

## 5. 竞争替代成本是什么？

通用计算过剩：SUI、AVAX、NEAR、ADA、DOT、INJ 抢同一类开发者，跨链桥与意图层降低迁移成本。赛道二十一个标的合计市值 `$419.0B`，其余十八个合计约 `$35B`。SOL 三十日链费用 `$23.11M`、年化 `$241.22M`；TRX 三十日 `$24.53M`、年化 `$355.61M`；以太坊一层三十日 `$12.45M`、年化 `$208.33M`。本链三十日仅 `$175.95K`、锁仓 `$13.66M`，替代成本只是换一条通用计算链。Canton 三十日 `$49.6M` 口径须另核，禁止并表。

## 6. 估值锚是否过大？

完全稀释估值/年化费用 `712.22x`（`$1.41B` 除以 `$1.98M`），高于 SOL `259x`，低于以太坊 `1412x`。市值/锁仓 `103.48x`：`$1.41B` 只对应 `$13.66M` 锁仓。一年 `-47.10%`、距高点 `-99.64%` 已压缩一轮，仍未收到费用能解释的位置。周期上 L1 贝塔大于比特币；相对比特币 `-18.87%` 是在重定价通用计算叙事。

## 7. 未来 12 个月必须发生 / 绝不能发生的事

必须看到年化链费用守住或高于 `$1.98M`，且 `$13.66M` 锁仓不被抽走，否则 `712.22x` 与 `103.48x` 只能靠叙事续命。绝不能再出现一年量级相对比特币跑输（本快照本币 `-47.10%`、相对 `-18.87%`）。协议收入、稳定币流通、最大供给与解锁若继续未知，捕获与供给主张无法被证伪。价格 `$2.54`、距高点 `-99.64%` 不是安全垫；历史低点 `$2.00` 只提醒回撤没有地板写在事实包里。

研究备忘，非投资建议。
