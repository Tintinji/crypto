# GRAM 空方（逻辑链 A）

快照 `2026-09-16T21:13:43Z`。市值排名 `#28`，流通市值 `$3.65B`，完全稀释估值 `$6.87B`。先声明：本币卖的是消费链区块空间，不是结算货币、高吞吐执行、支付轨道或机构隐私。空方只写反面：年化链费用 `$2.26M`、市值/锁仓 `68.23x`、一年相对比特币 `-36.81%`、近半供给未流通。

## 1. 这个资产在卖什么，谁付钱？

智能合约 L1 卖区块空间、状态承诺与生态 gravitas。付钱的应是终端用户与机器人的 gas。可核验链费用 30d 仅 `$84.95K`，1y 与年化均为 `$2.26M`。协议家族费用、协议收入、Llama 稳定币流通全部未知，持有人看不到可分配利润。CEX 托管 AUM 未知，不得当成协议锁仓。赛道把 GRAM/TON 标成消费链，与机构链不是同一市场。`$3.65B` 市值对照的是链费用口径，不是持有人收费权。

## 2. 需求是在扩张还是叙事空转？

一年本币 `-58.80%`，比特币同期 `-34.80%`，相对比特币 `-36.81%`。现价 `$1.31`，距历史高点 `$8.25` 回撤 `-84.16%`。三十日本币 `-1.40%`（相对 `-16.79%`）盖不住一年跑输；七日 `-4.50%`（相对 `-1.85%`）、二十四小时 `-1.01%` 也未扭转收缩。链上 DeFi 锁仓 `$53.57M`、市值/锁仓 `68.23x`：锁仓远低于 `$1B` 仍要 `$3B+` 市值，赛道默认叙事溢价。二十四小时成交额 `$31.20M`、成交额/市值 `0.85%` 是薄换手。协议家族锁仓与稳定币流通未知，禁止补成成长。

## 3. 代币如何捕获价值？

协议家族费用与协议收入全部未知，不得编造销毁、分红或回购。捕获取决于费用是否销毁或分给持有人；本包只能对照链费用。完全稀释估值/年化费用 `3042.92x`。市值/完全稀释估值 `0.53`，流通 `2.79B`、总供给 `5.25B`。年化费用远低于以太坊一层 `$208.33M`、SOL `$241.22M`、TRX `$355.61M`。日成交 `$31.20M` 是二级换手，不是票息。

## 4. 供给何时变差？

最大供给未知，公开接口未提供已验证解锁日历，不得编造解锁美元金额。不可验证性本身是论点。市值/完全稀释估值 `0.53` 说明尚未全流通，流通 `2.79B` 对总供给 `5.25B` 的缺口是已知浮筹。未知硬顶只说明没有上限可核对，不证明未流通部分何时进入，也不证明 `$2.26M` 年化费用能对冲净发行。

## 5. 竞争替代成本是什么？

消费链与机构链不是同一市场，也不是 ETH 结算位或 SOL 执行位。通用计算过剩：SUI、AVAX、NEAR、ADA、DOT、INJ 抢同一类开发者，跨链桥与意图层降低迁移成本。赛道二十一个标的合计市值 `$419.0B`，其余十八个合计约 `$35B`。SOL 三十日链费用 `$23.11M`、年化 `$241.22M`；TRX 三十日 `$24.53M`、年化 `$355.61M`；以太坊一层三十日 `$12.45M`、年化 `$208.33M`。本链三十日仅 `$84.95K`、锁仓 `$53.57M`，公开侧替代成本只是换一条链。Canton 三十日 `$49.6M` 口径须另核，禁止并表。

## 6. 估值锚是否过大？

完全稀释估值/年化费用 `3042.92x`（`$6.87B` 除以 `$2.26M`）。赛道禁止据此与 ETH `1412x`、SOL `259x` 并表称更贵，但本币自己的锚已经过大：市值/锁仓 `68.23x`，`$3.65B` 只对应 `$53.57M` 锁仓，锁仓不足 `$1B` 却要 `$3B+` 市值，默认叙事溢价。一年 `-58.80%`、距高点 `-84.16%` 已压缩一轮，仍未收到费用能解释的位置。周期上 L1 贝塔大于比特币；相对比特币一年 `-36.81%`、三十日 `-16.79%` 是在重定价消费链叙事。

## 7. 未来 12 个月必须发生 / 绝不能发生的事

必须看到年化链费用守住或高于 `$2.26M`，且 `$53.57M` 锁仓不被抽走，否则 `3042.92x` 与 `68.23x` 只能靠叙事续命。绝不能再出现一年量级相对比特币跑输（本快照本币 `-58.80%`、相对 `-36.81%`）。协议收入、稳定币流通、最大供给与解锁若继续未知，捕获与供给主张无法被证伪。价格 `$1.31`、距高点 `-84.16%` 不是安全垫；历史低点 `$0.519364` 只提醒回撤没有地板写在事实包里。

研究备忘，非投资建议。
